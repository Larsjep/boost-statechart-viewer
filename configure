#!/bin/sh

cd $(dirname $0)
TOP=$PWD

ln -sf ../../clang llvm/tools/clang
mkdir -p llvm/_build
cd llvm/_build
../configure --prefix=$TOP/_install

cat <<EOF
To compile LLVM run the following comands:
cd $PWD
make install
EOF
